<?xml version="1.0" encoding="utf-8"?>
<!-- Debug-only network security config.

     The Firebase Auth + Realtime Database emulators serve plaintext HTTP/WS on
     the host loopback alias 10.0.2.2 (Android emulator) / localhost. Android
     blocks cleartext traffic by default on API 28+, so this config permits it
     *only* for the local emulator hosts and *only* in debug builds (this file
     lives under src/debug, never src/main). Production release builds keep the
     platform default: cleartext remains blocked.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
